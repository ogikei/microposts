<h1>Followers</h1>

<% @followers.each do |follower| %>
    <div class="row">
      <ol class="followings">
        <li id="following-<%= follower.id %>">
          <%= link_to gravatar_for(follower, size: 50), follower %>
          <span class="user"><%= link_to follower.name, follower %></span>
        </li>
      </ol>
    </div>
<% end %>
